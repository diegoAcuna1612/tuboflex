---
// src/components/Features.astro
const features = [
  {
    // Icono Shield/Escudo
    iconPath: "M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z",
    title: "Barrera Total",
    desc: "Protección absoluta contra humedad y oxígeno, garantizando la estabilidad de fórmulas farmacéuticas.",
    color: "#e31e24", // Rojo
    bgColor: "rgba(227, 30, 36, 0.1)",
  },
  {
    // Icono Sun/UV (NUEVO)
    iconPath:
      "M12 12m-4 0a4 4 0 1 0 8 0a4 4 0 1 0 -8 0 M3 12h1m8 -9v1m8 8h1m-9 8v1m-6.4 -15.4l.7 .7m12.1 -.7l-.7 .7m0 11.4l.7 .7m-12.1 -.7l-.7 .7",
    title: "Bloqueo 100% UV",
    desc: "Evitamos reacciones químicas no deseadas bloqueando totalmente la radiación ultravioleta.",
    color: "#f59e0b", // Ámbar/Sol
    bgColor: "rgba(245, 158, 11, 0.1)",
  },
  {
    // Icono Flask/Pureza
    iconPath:
      "M10 2v7.31l-2.79 3.99A2 2 0 0 0 7.21 16.5h9.58a2 2 0 0 0 2-3.19L16 9.31V2m-6 0h6",
    title: "Alta Pureza >99.7%",
    desc: "Utilizamos discos de aluminio de primera fusión (slugs) para asegurar un envase inocuo de grado médico.",
    color: "#005daa", // Azul
    bgColor: "rgba(0, 93, 170, 0.1)",
  },
  {
    // Icono Gamma/Radioactividad controlada (NUEVO)
    iconPath:
      "M12 12m-1 0a1 1 0 1 0 2 0a1 1 0 1 0 -2 0 M2 12c0 5.5 4.5 10 10 10s10 -4.5 10 -10 M12 22v-10 M12 12l8.7 5M12 12l-8.7 5",
    title: "Esterilización Gamma",
    desc: "Material totalmente adecuado para procesos de esterilización por ionización gamma.",
    color: "#7c3aed", // Violeta
    bgColor: "rgba(124, 58, 237, 0.1)",
  },
  {
    // Icono Leaf/Hoja
    iconPath: "M12 2.69l5.66 5.66a8 8 0 1 1-11.31 0z",
    title: "100% Reciclable",
    desc: "El aluminio es infinitamente reciclable sin perder propiedades, apoyando la economía circular.",
    color: "#95c11e", // Verde
    bgColor: "rgba(149, 193, 30, 0.1)",
  },
  {
    // Icono Box/Hand (NUEVO)
    iconPath:
      "M7 11.5V14m0-2.5v-6a1.5 1.5 0 1 1 3 0V11m0-5.5v-1a1.5 1.5 0 1 1 3 0V11m0-6.5v-1a1.5 1.5 0 1 1 3 0V11m0-7.5a1.5 1.5 0 0 1 3 0v10.5a7 7 0 1 1-14 0V11.5a1.5 1.5 0 1 1 3 0z",
    title: "Ergonomía y Uso",
    desc: "Envase diseñado para facilitar la apertura, dosificación y almacenamiento del producto.",
    color: "#0891b2", // Cian
    bgColor: "rgba(8, 145, 178, 0.1)",
  },
];
---

<section class="features-section">
  <div class="container mx-auto px-4">
    <div class="header-center">
      <span class="tag">POR QUÉ ELEGIRNOS</span>
      <h2>
        Ingeniería detrás de <br />
        <span class="highlight">cada envase</span>
      </h2>
    </div>

    <div class="grid-container">
      {
        features.map((item) => (
          <div class="feature-card group">
            <div
              class="icon-wrapper"
              style={`--icon-color: ${item.color}; --bg-color: ${item.bgColor}`}
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="feature-icon"
              >
                <path d={item.iconPath} />
              </svg>
            </div>

            <h3 class="card-title">{item.title}</h3>
            <p class="card-desc">{item.desc}</p>

            <div class="hover-line" style={`background-color: ${item.color}`} />
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .features-section {
    padding: 6rem 0;
    background-color: #ffffff;
    position: relative;
  }

  /* Fondo de puntos sutil */
  .features-section::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: radial-gradient(#e5e7eb 1px, transparent 1px);
    background-size: 32px 32px;
    mask-image: linear-gradient(
      to bottom,
      transparent,
      black 10%,
      black 90%,
      transparent
    );
    z-index: 0;
    pointer-events: none;
  }

  .container {
    position: relative;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
  }

  .header-center {
    text-align: center;
    margin-bottom: 4rem;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .tag {
    color: #005daa;
    font-weight: 800;
    font-size: 0.8rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    background: rgba(0, 93, 170, 0.08);
    padding: 6px 16px;
    border-radius: 50px;
    display: inline-block;
    margin-bottom: 1rem;
  }

  h2 {
    color: #1e293b;
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .highlight {
    color: #005daa;
  }

  .subtitle {
    font-size: 1.1rem;
    color: #64748b;
    font-weight: 300;
  }

  /* Grid Layout */
  .grid-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 2rem;
  }

  /* Card Styles */
  .feature-card {
    background: white;
    padding: 2.5rem 2rem;
    border-radius: 16px;
    border: 1px solid #f1f5f9;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px -5px rgba(0, 0, 0, 0.1);
    border-color: transparent;
  }

  /* Icono */
  .icon-wrapper {
    width: 60px;
    height: 60px;
    border-radius: 12px;
    background-color: var(--bg-color);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    transition: transform 0.3s ease;
  }

  .feature-icon {
    width: 32px;
    height: 32px;
    color: var(--icon-color);
    stroke-width: 1.5;
  }

  .feature-card:hover .icon-wrapper {
    transform: scale(1.1) rotate(5deg);
  }

  /* Textos */
  .card-title {
    font-size: 1.35rem;
    font-weight: 700;
    color: #1e293b;
    margin-bottom: 0.75rem;
  }

  .card-desc {
    color: #64748b;
    line-height: 1.6;
    font-size: 0.95rem;
  }

  /* Línea animada inferior */
  .hover-line {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 4px;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s ease;
  }

  .feature-card:hover .hover-line {
    transform: scaleX(1);
  }
</style>
